<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.ex.HotelMapper">
<!-- 호텔예약 -->
	<insert id="hotelres">
		INSERT INTO hotelproduct(
			PID,
			ProductName,
			CheckIn,
			CheckOut,
			reDate,
			AdultCount,
			KidCount,
			SmallKidCount,
			AdultPrice,
			KidPrice,
			SmallKidPrice,
			ResName)
		VALUES(
			#{pid},
			#{productname},
			#{checkin},
			#{checkout},
			#{redate},
			#{adultcount},
			#{kidcount},
			#{smallkidcount},
			#{adultprice},
			#{kidprice},
			#{smallkidprice},
			#{resname})
    </insert>
    
    <!-- 호텔 출력 -->
	<select id="hotelList" resultType="HotelVO">
		SELECT * FROM hotelproduct ORDER BY PID DESC LIMIT #{Page}, #{PageSize}
	</select>
	
	<!-- 호텔예약 총 갯수 -->
	<select id="getHotelTotalCount" resultType="int">
		SELECT COUNT(*) FROM hotelproduct
	</select>
</mapper>